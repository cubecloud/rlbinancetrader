# PLAN — ветка feature/strategy_v6_to_rl

Цель: RL-надстройка над механической базовой стратегией из проекта sunday
с узкой свободой действий. НЕ end-to-end агент с нуля. Якорь — imitation
learning копии базовой стратегии, награда — настоящий P&L симулятора,
валидация — walk-forward с пермутацией и мультисидом, геометрия сравнения —
«чистая стратегия vs RL-агент» на одних и тех же данных.

Статус документа: v0.2 (2026-07-22). Обсуждается поэтапно; каждый этап
согласовывается с пользователем до начала работ по нему.

## Ключевые вводные v0.2 (после подтверждения look-ahead 22.07)

- В часовой семье фич sunday подтверждена утечка до 59 минут будущего
  (см. handoff/for_sunday_bug_2026-07-22.md и ответ from_sunday_reply_1.md).
  Честный v6 по фолдам ≈ +160% — ниже базы v5 (+198%). Прирост пакета v6
  (bounce + legflip) был утечкой.
- ПОЭТОМУ: базовая стратегия для этой ветки — ТА, КОТОРУЮ ОПРЕДЕЛИТ SUNDAY
  (v5, починенный v6 или иное; решение после paper-вердикта 2026-08-02).
  Ждём через файловую шину handoff/ (постоянный монитор включён 22.07).
- СТОП-лист до получения новой базы: числа AUX-линии июля (включая
  oracle-резерв выходов) недействительны; BC/RL-этапы не стартуют.
- ОБНОВЛЕНИЕ 23.07: каузальные STATE-parquet'ы ГОТОВЫ (from_sunday_reply_2):
  /home/cubecloud/Data/sunday_tests/state_v0/state_v3_causal_2024-03_2026-07.parquet
  и state_v3_causal_2021-01_2024-03.parquet (105 колонок, часовая семья через
  shift(1), slice-тест каузальности в билдере — PASS). Старые state_v3_2024-03_*
  и state_v3_2021-01_* — ЯДОВИТЫ, не использовать. Честные метки:
  judge_trades_causal.csv (141 сделка, +174.1%), judge_trades_causal_2021.csv
  (277, +167.8%); WinRate честной v6: 48%/39%.
- Сигнал для RL (предварительно, БЕЗ pre-reg — from_sunday_reply_2): на честных
  метках вето-фильтр ожил (XGB/PCA60, 4/5 сидов выше базы, +8.6 п.п. при ~90%
  сделок, пермутация чиста) — отделимый мусор есть и во ВХОДАХ, не только в
  выходах. Может расширить пространство действий агента; решение — после
  pre-reg подтверждения sunday и выбора базы.
- Работы, НЕ зависящие от выбора базы (можно вести сейчас): 0.1, 0.2, 0.4,
  каркас 1.1, проработка IL-подхода (1.2*).

---

## Архитектурные принципы (согласованы 2026-07-22)

- П1. Источник истины — база (PG): OHLCV + поминутные предсказания голов.
  Кэш (материализация окон) — только ускоритель. Манифест кэша: границы окна,
  версия билдера признаков, хэш.
- П2. Параметры стратегии (thresholds, SL, TP, cooldown, ...) — атрибут
  стратегии, не данные. Эталон — замороженный JSON пакета базовой стратегии.
- П3. Сигнальный тракт не переписываем: подключаем код sunday как библиотеку.
  Вторая реализация = вечный риск расхождения.
- П4. Observations проектируются отдельно под агента (стратегии они не нужны):
  рынок + внутренности тракта + контекст текущей сделки.
- П5. Multiprocessing и скорость закладываются с этапа 0; выбор ML-стека —
  по бенчмарку, не по вере.
- П6. Референс-зеркало sunday не ускоряем и не модифицируем — эталон парности.
- П7. Один код тракта на всех этапах (train → отбор → paper → live).
  «Один код» ≠ «одна семантика»: конвенции прибиваются assert'ами —
  «последний ЗАКРЫТЫЙ час» для часовых величин, index_type='target_time',
  NaT/staleness-гейт как в боевой стратегии, исполнение решения бара t
  по open(t+1) (отдельный unit-тест для НЕклонирующих действий).
- П8. Дисциплина экспериментов: pre-reg ДО прогона, одно изменение за раз,
  walk-forward (никаких shuffle), пермутационный контроль, мультисид 5,
  анти-мемоизационные меры (см. 3.3). Гейт — деньги в симуляторе против
  якоря, ПАРНЫЙ дизайн (см. 3.4). AP/accuracy ≠ деньги.
- П9. Реиспользуем инфраструктуру rlbinancetrader: BinanceEnvCash (step по
  предвычисленным признакам), spaces/маски, rewards, LabSubprocVecEnv,
  rllab.LabBase, экстракторы customnn. Не тащим: orderbook.py, старый
  онлайн-слой, MpCacheManager-сокеты.
- П10. Статистическая мощность — часть дизайна: на ~138 сделках непарное
  сравнение компаундов различает только эффект ≥ +0.89%/сделку (нереально
  для тайминга выходов); парный per-trade тест различает +0.12–0.48%/сделку.
  Все гейты строятся парными; расписание входов при оценке замораживается,
  полный прогон симулятора (cooldown/CB пересчитаны) репортится рядом.

---

## Этап 0 — Фундамент: данные, стратегия-модуль, стек

- 0.1 [независимо от базы] Окружение: conda-env (py3.10; SB3 2.3.2 как
      отправная точка). Проверить совместимость pandas<2 / gymnasium 0.29.1
      со стеком-кандидатами (SBX/JAX); при необходимости — env посвежее.
- 0.2 [независимо от базы] Data-слой: доступ к PG (OHLCV spot BTCUSDT 1m +
      предсказания голов), интерфейс «окно → (ohlcv_df, features_df)».
      Кэш-материализация с манифестом (П1). NaT-guard на живом крае.
      Каузальность часовых производных — «последний закрытый час» + тест
      каузальности на срезе (значение на минуте t не меняется при обрезке
      данных до t).
- 0.3 [ЖДЁТ БАЗУ] Strategy-модуль: механика базовой стратегии sunday как
      импортируемая библиотека (пошаговая логика входов/выходов, параметры
      из frozen JSON). Гейт 0.3: parity-тест против эталона sunday на
      каузальном state (допуски согласовать числом до прогона; правило
      одной полосы: сравнения только на одном билде state).
- 0.4 [СДЕЛАНО 23.07, детали: strategy_rl/bench_results.json] Бенчмарк на
      реальном окне (июнь-2026, 43200 минут, 93 фичи), CPU станции, 8 сред:
      raw step 754k шаг/с; вектор: SB3 Dummy 219k / SB3 Subproc 37k /
      Puffer Serial 339k / Puffer MP 712k; обучение PPO: SB3 12.9k ≈ SBX
      12.5k шаг/с. ВЫВОДЫ: (а) среда на предвычисленных массивах НЕ узкое
      место — C-переписывание не требуется (П5 закрыт цифрой); (б) узкое
      место — PPO-update (12k), а не шаг среды; (в) SBX на CPU выигрыша
      не дал, SubprocVecEnv вреден для быстрой среды (IPC дороже шага).
      РЕШЕНИЕ: базовый стек — SB3 (+ MaskablePPO, которого в SBX нет).
      ДОЗАМЕР 0.4b (23.07, бюджет 10 ядер/20 потоков на обеих машинах,
      jarvis в docker, файлы bench_*.json):
      • Вектор сред: LabSubprocVecEnv (sb3-rllab, дизайн пользователя)
        станция 171k/218k (20×16/20×64) против 30k у штатного subproc;
        jarvis 58k/76k против 11k. Подтверждена амортизация IPC пачкой;
        чем больше сред на процесс — тем лучше.
      • PPO-обучение (MlpPolicy 90-вход, n_steps=256, batch=512):
        станция CPU: Dummy-8 12.9k → lab20×64 30.5k шаг/с (×2.4);
        jarvis CPU: Dummy-8 4.9k → lab20×64 11.5k (×2.4);
        jarvis GPU0 (3060 12GB): Dummy-8 2.3k, lab20×64 7.8k — GPU
        МЕДЛЕННЕЕ CPU на этой платформе для маленькой сети (Westmere:
        медленный однопоток, PCIe 2.0 — оверхед пересылок/запуска ядер
        съедает выигрыш). torch 2.2.0+cpu и +cu118 работают на Westmere
        без SIGILL (SSE-фолбэк, микротесты пройдены).
      РЕШЕНИЕ-2: тренировочная конфигурация — LabSubprocVecEnv 20×64;
      CPU-обучение по умолчанию (станция 30.5k / jarvis 11.5k шаг/с =
      полный проход истории 2.9M минут за 95с / 4.2мин); GPU jarvis
      переоценить ТОЛЬКО когда появится тяжёлая сеть (Hybrid/LSTM,
      большие батчи) — перемер с реальной архитектурой перед этапом 2.
- 0.5 [ЖДЁТ каузальный state + базу] Квантификация oracle-резерва ЧИСЛОМ:
      зеркалом — (а) идеальный выход внутри сделки, (б) идеальный при
      ограничении «только раньше штатного», (в) «±N минут от штатного».
      Обе эпохи отдельно. Если (б) < ~0.5%/сделку — свобода «только раньше»
      мертва: менять дизайн действий или закрывать RL-затею. РЕШАЮЩИЙ гейт
      жизнеспособности проекта.
- 0.6 [ЖДЁТ базу] Механический бейслайн выходов: сетка trailing-stop /
      time-stop / «выход при откате X% от пика сделки»; подбор на train,
      прогон на валидации. RL обязан бить не только базу, но и лучший
      простой твик (урок sunday: «брала только механика»).

## Этап 1 — Среда (gym) поверх data-слоя и strategy-модуля

- 1.1 [каркас — независимо от базы] Режимы snapshot (обучение) и online
      (paper/live поверх свежих строк PG). База — BinanceEnvCash с заменой
      источника данных (П9). Trade-book фичи (tb_*) среда вычисляет из
      трейдбука АГЕНТА (предвычисленные — только в IL-датасете).
- 1.2* [ПРОРАБОТАТЬ ДО ФИКСАЦИИ ACTION SPACE] Дизайн imitation learning:
      action space производен от выбранного IL-подхода. Варианты на
      обсуждение (готовлю сравнение отдельным документом):
      а) чистый BC (supervised: state → действие эксперта);
      б) BC + DAgger-подобное дообучение (эксперт-разметчик доступен —
         механика детерминирована, разметка любых состояний бесплатна);
      в) offline RL поверх экспертных траекторий (CQL/IQL/AWAC);
      г) BC-якорь + on-policy RL fine-tune с KL (исходный план).
      Вместе с выбором фиксируются: пространство действий (какая свобода
      и где), маскирование, обработка cooldown/CB при отклонениях
      (полный replay: изменённое действие → пересчёт последующих входов).
      Ограничение сохраняется: SL/circuit-breaker у агента не отбираются.
- 1.3 Observation-дизайн (отдельное обсуждение):
      1.3.1 группы: рынок (производные), тракт (уверенности голов, отступ
            от порога, режим, нога — в каузальной семантике), контекст
            сделки (нереализованный P&L, длительность, откат от пика,
            расстояние до SL/TP) + tb_* от агента;
      1.3.2 нормализация: скейлер только из train-окна, артефакт с хэшем
            в манифесте (П1), байт-в-байт переиспользуется online; запрет
            сырых абсолютных цен в obs (только относительные формы);
            assert покрытия: p1–p99 валидационных фич внутри train-диапазона;
      1.3.3 lookback: нужен ли, глубина, формат (вектор vs 2D).
- 1.4 Reward: log(1+r) ЗАКРЫТОЙ сделки (сшито с компаунд-гейтом);
      исполнение — next-bar-open; sensitivity-прогон с проскальзыванием
      0/1/2 bps; ЗАПРЕТ shaping на нереализованном P&L (если понадобится —
      только potential-based и только через pre-reg).
      Гейт 1.4 (smoke): политика «повторяй эксперта» воспроизводит сделки
      и P&L strategy-модуля бит-в-бит.
- 1.5 Векторизация: выбранный стек, прогрев кэша, замер пропускной
      способности на полном окне.

## Этап 2 — IL-клон базовой стратегии (якорь) [ЖДЁТ базу и 1.2*]

- 2.1 Экспертный датасет: траектории базовой стратегии (observation,
      действие); разметка — из strategy-модуля. Эпохи 2021-2024 и 2024+
      можно смешивать ДЛЯ IL (клонируются действия детерминированной
      политики, не исходы) — зафиксировано отдельной строкой; для RL-этапа
      запрет смешивания сохраняется.
- 2.2 Обучение IL-головы. Гейт 2.2 (pre-reg, детерминированные пороги —
      НЕ «статистически неотличим», это проходится пустотой при малой
      мощности): совпадение действий ≥99.x% по минутам, идентичное
      множество сделок, |Δreturn|/сделку и |Δкомпаунд| < порогов,
      зафиксированных до обучения.
- 2.3 Мультисид IL (5 сидов): разброс метрик повторения — в допуске.

## Этап 3 — RL-надстройка [ЖДЁТ 0.5: резерв должен существовать]

- 3.1 Pre-reg документ: критерии успеха/провала, окна, сиды, контроли,
      KL-коэффициент, «пол полезности» (см. 3.2) — ДО первого прогона.
- 3.2 RL fine-tune от IL-весов: свобода по решению 1.2*, KL-штраф к якорю.
      «Пол полезности»: если медианный |сдвиг решения| или медианная парная
      дельта ≈ 0 — вердикт «эффекта нет, KL задушил», а НЕ «не хуже якоря —
      деплоим». KL не подбирается глядя на валидацию.
- 3.3 Контроли: пермутация наград; мультисид 5; нижний бенчмарк «выход в
      случайный момент» (RL обязан его бить до разговоров о якоре); верхняя
      планка — честный oracle из 0.5 (репортим долю забранного зазора);
      anchor-gap мониторинг (KL и доля расхождений по фолдам; рост при
      плоской награде = стоп); leave-top-k-out (гейт выживает без top-2
      сделок по вкладу); канарейка утечки (кандидат на state со сдвинутым
      часом: обвал = политика ела утечку, брак). Анти-мемоизация: temporal
      holdout внутри train, джиттер стартов эпизодов, trade-dropout,
      ограничение ёмкости. Пермутация и мультисид от заучивания траектории
      НЕ защищают — поэтому блок обязателен.
- 3.4 Геометрия walk-forward (принцип: сравнимость «чистая стратегия vs
      RL» на одних данных, парный дизайн П10):
      (а) IL-обучение: обе эпохи;
      (б) RL-train: 2021-01..2024-03 (окно in-sample для тракта — правильное
          место «сжигать»);
      (в) отбор/тюнинг: фолды внутри 2024-03..2026-01 — окно помечено
          «anchor-inflated» (hyperopt базы делался на его части), только
          относительные сравнения;
      (г) финальный holdout, вскрывается ОДИН раз по pre-reg:
          2026-01..2026-07 (+paper дальше);
      (д) на каждом разрезе — парная статистика per-trade дельт
          (Wilcoxon/sign + block bootstrap дельты компаунда) И полный
          прогон симулятора; правило агрегации сидов: медиана проходит
          порог, худший сид не хуже согласованного предела.
      Точные границы пересматриваются под окна калибровки НОВОЙ базы.

## Этап 4 — Отбор и путь в прод [ЖДЁТ этап 3]

- 4.1 Кандидаты прогоняются через референс-зеркало sunday (П6) на
      непрерывной полосе, на каузальном state.
- 4.2 Финальный гейт (парный, из pre-reg): минимальный практический эффект
      порядка +0.10–0.15%/сделку парно; tail-guard: доля компаунда top-10
      сделок у RL не ниже, чем у базы (агент не срезал винеров); MaxDD не
      хуже согласованного предела. Не прошёл — фиксируем и НЕ деплоим.
- 4.3 Деплой-контур (решить ДО старта 4.4): а) FreqAI-RL; б) предсказания
      головы в PG, стратегия читает (как сейчас в sunday). Онлайн-режим
      среды обслуживает оба.
- 4.4 Paper: ДВА гейта. Гейт-A операционный (~2 недели): parity решений,
      латентность, NaT-край, сверка paper-сделок с зеркалом на том же куске.
      Гейт-B денежный: по ЧИСЛУ сделок (минимум ~30 парных наблюдений,
      при ~1 сделке в 6 дней это ~полгода), не по календарю. Shadow-режим
      с первого дня: политика логирует свои решения рядом с живой базой,
      не торгуя — парная live-статистика бесплатно. Kill-switch: откат на
      механику базы по заранее заданным условиям (аномальное решение,
      live-KL выше порога, деградация признаков).

---

## Ресурсы

- GPU jarvis (fridai-jarvis, 192.168.1.145): 4 GPU — 1× RTX 3060 12GB +
  3× RTX 3060 Ti 8GB. С 23.07 прод-inference sunday закреплён за GPU1
  (3060 Ti, по uuid; выполнено sunday-сессией, from_sunday_reply_3;
  откат-контейнер сохранён). СВОБОДНЫ под трейны: GPU0 (12GB), GPU2, GPU3
  (8GB) — без чужих контекстов. VRAM прод-инференса: пик 1.2GB из 8GB.
  GPU станции занят прод-контуром и трейнами sunday.

## Открытые вопросы

1. IL-подход и производный от него action space (1.2*) — обсуждение по
   готовности моего сравнительного документа.
2. Точные границы геометрии 3.4 — после выбора базовой стратегии sunday.
3. Пороги гейтов 2.2 / 4.2 — числа до соответствующих прогонов.
4. Разрешено ли агенту держать позже сигнального/legflip-выхода базы
   (не только «раньше») — письменно решить до этапа 1.
5. GPU-планирование этапа 2/3 (какие карты jarvis можно занимать и когда).

## Зависимости от sunday (шина handoff/, монитор активен)

- Новая базовая стратегия (после paper-вердикта 2026-08-02) + frozen JSON.
- Каузальные STATE-parquet'ы (пересборка анонсирована в from_sunday_reply_1).
- Пересчёт вето-серии и AUX-линии на каузальном state.

## Правила работы (выжимка договорённостей)

- Никаких подписей/атрибуции Claude в git (коммиты, push, PR).
- «А можем X?» = обсуждение, не команда запуска. Дорогие прогоны — только
  после явного «запускай»; параметры согласовываются ДО.
- Старый код не ломать: копировать-переименовывать; правки существующих
  файлов — diff и «да» пользователя; чекпоинт-коммит перед правками.
- Длинные прогоны — tmux, логи через FileHandler; после старта — доклад
  первой эпохи; далее статусы.
- Общение: русский, просто, с числовыми примерами; техтермины не переводить.

## История версий

- v0.1 (22.07): первый черновик, 5 этапов.
- v0.2 (22.07): коррекции двух критиков (парные гейты П10, oracle-резерв 0.5,
  мех. бейслайн 0.6, анти-мемоизация 3.3, чистый holdout 3.4г, shadow/два
  гейта 4.4, train/serve asserts П7); вводные после подтверждения look-ahead
  (база = решение sunday, стоп-лист данных); IL-подход определяет action
  space (1.2*); геометрия — сравнимость «чистая стратегия vs RL» (3.4);
  ресурсы GPU jarvis.
