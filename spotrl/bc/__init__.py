"""Этап копирования поведения (Behavior Cloning) v7 — сборка датасета клонирования.

Что здесь: прогон эксперта v7 ЧЕРЕЗ среду (режим копирования, cb_equity_mode=
v7_ledger) и сбор на каждом баре пары (наблюдение v2, экспертное действие головы
0). Наблюдение берётся ИЗ среды (`env.observe()`), а не пересчитывается отдельно.

Чего здесь НЕТ: обучения. Обучение запускается ОТДЕЛЬНО после утверждения
pre-reg порогов гейта копирования пользователем (дисциплина П8).
"""
