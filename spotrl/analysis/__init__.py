"""spotrl.analysis — офлайн-замеры жизнеспособности RL-надстройки.

Первый обитатель (этап Э0.2, PLAN v0.5): измерение oracle-резерва свободы
раннего выхода из dip-сделок. Пакет ТОЛЬКО читает `strategy_rl` (наследует
хук, импортирует разметку) и не правит его. Новый код механики стратегии
здесь не появляется — только измерение фактов.
"""
