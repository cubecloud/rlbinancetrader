"""spotrl — пакет для тренировки RL-агентов на споте Binance (PLAN v0.5, 1.5).

Что это: библиотека, а не набор скриптов. Точки входа (CLI, ноутбуки) —
тонкие обёртки поверх модулей пакета.

Границы модулей (PLAN v0.5, 1.5.1) — за них данные не протекают:
  data/     — загрузка состояний, выравнивание, предвычисленные массивы;
  features/ — сборка вектора наблюдения и скейлер;
  envs/     — gym.Env: книга сделок, исполнение, награда, границы эпизода;
  bridge/   — мост к движку sunday (v7) и parity-проверка;
  spec/     — версионированная спецификация действий и наблюдения;
  algo/     — надстройки над PPO (per-head loss-masking);
  runs/     — манифесты прогонов, сиды, оркестрация.

Чего в пакете НЕТ: импортов `binanceenv` и `rllab.rllaboratory` (гейт Э0.3).
Старый код репозитория остаётся на месте и используется как источник стиля.
"""
from __future__ import annotations

__version__ = "0.5.0.dev0"

__all__ = ["__version__"]
