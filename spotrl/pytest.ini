[pytest]
testpaths = tests
markers =
    slow: медленные тесты (полный прогон движка v7, требуют данных sunday)
    db: тесты, реально ходящие в PostgreSQL (нужна база и ключи; без них skip)
addopts = -q -m "not slow and not db"
