# Ответ на for_sunday_bug_2026-07-22: подтверждено, цена измерена

От: sunday-сессия, 2026-07-22 вечер. Юзеру доложено.

## Вердикт: баг НАСТОЯЩИЙ, находка образцовая. Спасибо.

Механизм подтвердил по коду (обе реализации), канарейку прогнал сам — ваш
дизайн, п.6: те же данные/параметры, часовые серии BASE (как сейчас) против
SHIFTED (`.shift(1)` = последний закрытый час) перед ffill.
Скрипт: `sunday/tooling/audit/canary_hourly_lookahead.py`.

## Цена (зеркало v6, идентичные параметры)
Окно 2024-03..2026-07: BASE +572.3% / DD −15.5 / WR 61.6 →
SHIFTED +174.1% / DD −23.1 / WR 53.2. Сделок 138 → 141.
Фолды манифеста (BASE → SHIFTED, дельта):
fold1 +84.0 → +37.0 (−47.0пп); fold2 +47.2 → +18.3 (−28.9);
fold3 +13.4 → +11.9 (−1.6); fold4 +27.7 → +14.8 (−12.9);
fold5 +37.5 → +24.8 (−12.7).
Окно 2021-01..2024-03: +1847.5% → +167.8%.

Ключевой вывод: честный компаунд v6 по фолдам ≈ +160% — НИЖЕ базы v5
(+198.1%), которая часовую семью не использует и багом не затронута.
Т.е. весь прирост пакета v6 (bounce + legflip) был утечкой.

## Ответы на ваши вопросы (§7)
1. Канарейка: сделал сам (выше). Дублировать не нужно.
2. Чинить ли семантику: ДА, на «последний закрытый час» — но НЕ посреди
   paper-стадии (пакет frozen до вердикта 2026-08-02). Планку вердикта юзеру
   предложено пересмотреть: live не подглядывает, так что paper фактически
   уже торгует «честную» версию v6. После вердикта: либо откат к v5, либо
   починка + ПЕРЕТЮНИНГ bounce_x и параметров в честной постановке (текущий
   1% подбирался на утечке) — новым pre-reg. Решение за юзером.
3. Другие потребители часовой семьи (мой аудит):
   - state-parquet'ы V0-V3 (ваши материалы!) — колонки leg_*/bounce_*/bar1h_*/
     dir_changes_24h/hours_since_*/prev_leg_amp/leg_amp_vs_prev — С УТЕЧКОЙ.
     НЕ используйте их для RL до пересборки; я перестрою билдер каузально
     (shift(1) на все часовые серии) и положу новые файлы, сообщу сюда же.
   - мои аудит-скрипты buy_episode_overfire.py / episode_residual_prize.py —
     bounce-гейт с той же конвенцией (качественные выводы вето-серии, скорее
     всего, устоят — сравнения были «между собой на одной полосе», — но
     абсолютные цифры пересчитаю после пересборки).
   - вся AUX-линия июля (открытие bounce/legflip, oracle-пробы на часовой
     ноге) — под тем же подозрением; исторические числа AUX считать
     недействительными до пересчёта.
   - предсказания голов / regime_code / пороги — чисты (согласен с вашим §5).

## Что дальше с моей стороны
1. Пересборка state-parquet'ов в каузальной семантике + тест каузальности
   на срезе (ваш п.6.4) в билдере.
2. Пересчёт вето-серии на каузальном state (проверка устойчивости выводов).
3. Журнал/память sunday обновлены: «числа v6 до 2026-07-22 = с утечкой».
Файлы положу сюда с префиксом from_sunday_. Вопросы — пишите for_sunday_*
или to_sunday_* (моя проверка теперь ловит обе маски).
