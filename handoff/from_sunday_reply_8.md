# ОТ SUNDAY (reply 8): supervised early-SL-exit — ВЫПОЛНЕНО, вердикт RED на tune-OOS

От: Sunday-сессия, 2026-07-29. Результат по задаче `for_sunday_supervised_sl_targets_2026-07-29.md`.
Приоритет дал юзер; прошли S0-S4 последовательно с pre-reg. holdout 2026-01..07 НЕ тронут.

## Что сделано (все стадии воспроизвели ваши числа бит-в-бит)
- S1 датасет (метка привязана к v7, вариант 1): 245 dip / 37 SL, reason_match=1.0, split-by-trade.
- S2 p_SL воспроизвёл K3: GBM OOS-AUC 0.848 CI[0.735,0.929], shuffle 0.48, топ-фичи те же
  (a_peak_unreal, w_equity_drawdown, a_entered_on_up, a_unreal_pnl). early-half слабее (0.70) — ваша оговорка #2.
- S3 прибор валиден: ORACLE Σ+648.71 CI[+556,+747] проходит, RANDOM Σ-55.73 доля>0=0.00 валится.
- S0 MDE: гейт различает ~2% oracle-резерва (Σ*≈+6.4пп, +0.17пп/SL-сделку), запас 7.45× — мощность НЕ узкое место.

## S4 ВЕРДИКТ (tune-OOS 2024-03..2026-01) = RED
p_SL (GBM) обучен на 2021; (τ,k) выбраны по гейту на train (best τ=0.8 k=3, in-sample sum +133, EFFECT=True);
ОДИН прогон на tune-OOS:
- A: sum **-24.40пп** CI[-67.23,+14.53] pass=False | B: med -1.79 p=0.91 pass=False | acted 17/118.
- **SUCCESS=False.** Справка: ВСЕ 15 пар (τ,k) на OOS отрицательны (sum -21..-40) — не промах выбора τ.

## Механизм (K2) — важно для вас
На tune-OOS SL-сделок всего **8**; политика затронула 4, Σ_SL = **+21.05пп** — на НАСТОЯЩИХ SL сигнал
РАБОТАЕТ. Но не-SL (восстанавливающихся) 110, ложные ранние выходы = **-45.46пп**. Нетто -24.
То есть подтверждается ваш же вывод: связывающее ограничение — ТОЧНОСТЬ (не дёргать восстанавливающиеся),
не мощность. Плюс сдвиг режима: bull-окно 2024-26 даёт всего 8 SL-сделок → резерв тонок, драг доминирует.

## Честный вывод (по pre-reg — не тюним дальше)
Сигнал реален (AUC 0.85, +21 на SL), но как денежный оверлей над v7 в OOS не проходит: precision p_SL
недостаточна, драг ложных выходов превышает резерв. НЕ подгоняем τ/k/модель (pre-reg запрет).
Что могло бы помочь (НЕ делаем без вашего/юзера решения): (1) снизить ложные выходы — метка «SL И
oracle-резерв>0» вместо просто SL; (2) cost-sensitive порог per-trade; (3) больше SL-сделок (окно
2018-2021) для устойчивости; (4) вернуть RL с этой меткой как shaping. Все — отдельным pre-reg.

Артефакты Sunday: tooling/audit/{plan,prereg,mde_sl_result,s2_reachability_result,s3_calibration_result,
s4_verdict,s4_result}_*, sl_exit_{dataset,policy,train_s2,train_s4}.py, unitests/test_sl_exit_*.py.

— Sunday
