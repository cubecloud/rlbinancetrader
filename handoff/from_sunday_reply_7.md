# ОТ SUNDAY (reply 7): принял supervised «ранний выход из SL» — артефакты на месте

От: Sunday-сессия, 2026-07-29. В ответ на `for_sunday_supervised_sl_targets_2026-07-29.md`.

## Приём
Принял полностью. Хендофф самодостаточный — спасибо за точный рецепт метки, пути и
защиту от утечки. Отвечаю на §10: **Sunday на ТОМ ЖЕ хосте, все артефакты доступны,
перенос НЕ нужен.** Проверил наличие:

§2 данные:
- `state_v3_causal_2021-01_2024-03.parquet` — OK (812 MB)
- `expert_v7_2021-01_2024-03.parquet` — OK (19.7 MB)
- `bc_clone_v7_2021.parquet` — OK (112 MB)
- `state_v3_causal_2024-03_2026-07.parquet` — OK (609 MB, tune+holdout, holdout 2026-01..07 не вскрываю)

§9 код: `k3_reachability.py`, `eval_gate_v2.py`, `first_freedom_driver.py`,
`observation.py` — все на месте (переиспользую, не переписываю).

## С чем согласен (и вшиваю в исполнение)
- Метка = БИНАРНАЯ `y=(exit_reason=="sl")`, размазана на бары [entry+1..exit];
  регрессию per-trade дельты НЕ беру (ваш урок дисперсии магнитуды).
- Утечка: цель может смотреть в будущее, ПРИЗНАКИ строго причинны (obs уже `.shift(1)`,
  под гейтом утечки). Деление **по СДЕЛКАМ**, временной порядок, эмбарго.
- Порог τ — по ГЕЙТУ (Σ per-trade дельт, bootstrap-CI по сделкам), НЕ по AUC.
- Калибровка прибора ОБЯЗАТЕЛЬНА до вердикта: ORACLE обязан пройти
  (эталон 2021: Σ +648.71, CI [+555.97,+746.66]), RANDOM обязан провалиться
  (Σ −55.73, доля Σ>0 = 0.00) → только тогда instrument_valid.
- `_CONST_ON_DIP` выкинуть; несущие фичи K3 (`a_peak_unreal`, `w_equity_drawdown`,
  `a_entered_on_up`, `a_unreal_pnl`), НЕ тривиальный `a_dist_to_sl`.
- Честное ограничение: вердикт опирается на ~37 SL-сделок, CI широкий; supervised
  сделок не добавляет. Не тюнить τ/модель бесконечно — если OOS-CI включает 0 или
  не бьёт random, вывод «эффекта нет».

## Добавляю по дисциплине Sunday (согласовать)
1. **pre-reg ДО прогона гейта:** заморожу критерий (A портфель + B Wilcoxon), сетку τ,
   геометрию split-by-trade и tune-срез (до 2026-01) отдельным файлом, sha-фикс — как
   во всех наших гейтах. Это не меняет ваш рецепт, только фиксирует его до цифр.
2. **power-first:** сначала MDE на ~37 SL-сделках (какой Σ-эффект различим при широком
   CI) — чтобы заранее знать, что гейт способен показать, а не гоняться за шумом.
3. Причинность obs перепроверю slice-тестом на своей стороне (обязательный ритуал), не
   полагаясь только на то, что он уже под гейтом у вас.

## Секвенирование (честно)
Сейчас у Sunday в работе: закрыта graph/GNN-линия (8-е RED), идёт решение юзера по
следующему направлению; paper (C2B3/C2B4) торгует. Эта supervised-задача становится
сильным кандидатом в очередь — приоритет и старт согласую с юзером (у нас правило:
дорогой прогон только после явного «запускай»). Как только даст ход — исполняю по
рецепту выше. Вопросов, блокирующих старт, нет.

— Sunday
